# GitHub Configuration (Optional)
# Use this if you want ADW to use a different GitHub account than your default 'gh auth' login
# If not set, ADW will use whatever GitHub account is authenticated with 'gh auth login'
GITHUB_PAT=github_pat_YOUR_PAT_HERE

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Priority: Anthropic (if enabled) > OpenAI (if enabled)
# At least one provider must be enabled with a valid API key
#
# Option 1: Use Claude Code CLI (Anthropic)
#   - Set ANTHROPIC_ENABLED=true and provide ANTHROPIC_API_KEY
#   - Full functionality including slash commands and file operations
#
# Option 2: Use OpenAI API
#   - Set OPENAI_ENABLED=true and provide OPENAI_API_KEY
#   - LLM prompt execution only (no slash commands or file operations)
#
# You can enable both providers - Anthropic will take priority if both are configured
ANTHROPIC_ENABLED=true
OPENAI_ENABLED=false

# Anthropic Configuration
# Required when ANTHROPIC_ENABLED=true
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-YOUR_KEY_HERE

# OpenAI Configuration
# Required when OPENAI_ENABLED=true
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-YOUR_OPENAI_KEY_HERE

# =============================================================================
# Claude Code CLI Configuration
# =============================================================================
# Path to Claude Code CLI executable
# If 'claude' does not work, run 'which claude' (Linux/Mac) or 'where claude' (Windows)
# and paste the full path here
CLAUDE_CODE_PATH=claude

# Returns Claude Code to the root directory after every command
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# =============================================================================
# Optional Services
# =============================================================================
# Agent Cloud Sandbox Environment (E2B)
E2B_API_KEY=

# Cloudflare Tunnel token for exposing webhook endpoint to the internet
# Required if using trigger_webhook.py with GitHub webhooks
CLOUDFLARED_TUNNEL_TOKEN=